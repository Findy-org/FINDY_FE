export type Props = {
  /**
   * The variant of the Chip.
   *  @default 'large'
   */
  variant: 'small' | 'medium' | 'large';
  /**
   * content to be displayed within the Chip component.
   */
  children: string;
};
